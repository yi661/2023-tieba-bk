# Veaury混合框架环境搭建指南

## 环境要求

在开始项目开发之前，请确保您的系统满足以下要求：

### 必需软件
1. **Node.js** (版本 16.0 或更高)
   - 下载地址: https://nodejs.org/
   - 推荐版本: 18.x LTS

2. **npm** (随Node.js自动安装)
   - 安装Node.js后，npm会自动安装

3. **Git** (版本控制)
   - 下载地址: https://git-scm.com/

### 可选软件
1. **Visual Studio Code** (推荐IDE)
   - 下载地址: https://code.visualstudio.com/

2. **Chrome浏览器** (开发调试)

## 安装步骤

### 1. 安装Node.js

1. 访问 [Node.js官网](https://nodejs.org/)
2. 下载并安装 **LTS版本** (长期支持版本)
3. 安装过程中选择默认选项即可
4. 安装完成后，打开命令提示符或PowerShell验证安装：

```bash
node --version
npm --version
```

如果显示版本号，说明安装成功。

### 2. 配置项目依赖

在项目根目录下执行以下命令安装依赖：

```bash
# 安装项目依赖
npm install

# 或者使用yarn (如果已安装)
yarn install
```

### 3. 开发环境启动

安装完成后，可以启动开发服务器：

```bash
# 启动开发服务器
npm run dev

# 或者
npm run serve
```

服务器启动后，访问 http://localhost:3000 查看应用。

## 项目结构说明

```
2023-tieba-bk/
├── src/                    # 源代码目录
│   ├── components/         # 组件目录
│   ├── pages/             # 页面组件
│   ├── stores/            # 状态管理
│   ├── utils/             # 工具函数
│   ├── types/             # TypeScript类型定义
│   ├── styles/            # 样式文件
│   ├── router/            # 路由配置
│   └── main.ts           # 应用入口
├── public/                # 静态资源
├── package.json          # 项目配置
├── vite.config.ts        # Vite配置
├── tsconfig.json         # TypeScript配置
└── index.html           # HTML模板
```

## 技术栈说明

### 前端框架
- **Veaury**: Vue 3 + React 18 混合框架
- **Vue 3**: 组合式API，响应式系统
- **React 18**: 并发特性，Hooks系统

### 状态管理
- **Pinia**: Vue状态管理 (替代Vuex)
- **Redux Toolkit**: React状态管理

### 路由
- **Vue Router**: Vue应用路由
- **React Router**: React应用路由

### UI组件库
- **Element Plus**: Vue组件库
- **Ant Design**: React组件库

### 构建工具
- **Vite**: 快速构建工具
- **TypeScript**: 类型安全
- **SCSS**: CSS预处理器

## 开发命令

```bash
# 开发模式
npm run dev

# 生产构建
npm run build

# 预览生产版本
npm run preview

# 代码检查
npm run lint

# 类型检查
npm run type-check
```

## 常见问题

### Q: npm install 失败怎么办？
A: 尝试以下解决方案：
1. 清除npm缓存: `npm cache clean --force`
2. 删除node_modules文件夹和package-lock.json
3. 重新运行 `npm install`
4. 使用淘宝镜像: `npm install --registry=https://registry.npmmirror.com`

### Q: 端口3000被占用怎么办？
A: 修改vite.config.ts中的端口配置，或使用其他端口：
```bash
npm run dev -- --port 3001
```

### Q: TypeScript类型错误怎么办？
A: 检查tsconfig.json配置，确保类型定义正确导入。

## 下一步

环境搭建完成后，您可以：
1. 查看项目文档了解功能设计
2. 运行开发服务器开始开发
3. 根据需求文档实现具体功能

如有问题，请参考项目文档或联系开发团队。